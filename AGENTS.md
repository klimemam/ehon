# このリポジトリに書き込むエージェントへ

## -1. ファイル構成（2026-08-17 変更）

`files/` を解体した。トップ直下に置く。

```
AGENTS.md        この文書
README.md        リポジトリの説明
index.html       索引
docs/            始点（私的な分析）。thesis.md, open-questions.md, market.md, agent-economy.md
works/           作品
├── README.md    規約16条 ＋ 索引（入口）
├── <slug>/      各作品。vN.md と critiques/
└── common/      作品をまたぐもの。方法・地図・種・記録
```

**作品ごとの批評・関連資料は、その作品の `critiques/` に置く。** 作品をまたぐものだけが `common/` に入る。

## 0. どのブランチに書くか

**2026-08-16 に運用を変えた。** それまでは `planning` で作業し `main` へ移していたが、`planning` は廃止した。**同じ日に GitHub Pages の公開も停止した。**

| ブランチ | 用途 |
| --- | --- |
| **`main`** | **作業。ここに書く。** 草稿・批評・改稿・覚書、すべて |
| `release-rev<数字>` | **発表の単位。** 一度切ったら書き換えない |

**公開はしていない。** GitHub Pages は 2026-08-16 に停止した（設定を削除。`.nojekyll` と `index.html` はそのまま残してある）。再開するときは、配信元をどの枝にするかを先に決める。

### 作業の手順

**`main` へ直接 push しない。** 枝を切り、PR を作ってからマージする。

```bash
git checkout main && git pull
git checkout -b <作業名>
# 書く
git push -u origin <作業名>
gh pr create --base main --head <作業名>
gh pr merge <番号> --merge --delete-branch
```

枝の名前は作業の内容にする（`restore-kabe-v1`、`fable-critique` など）。

### リリース

発表する時点で、そのときの `main` から枝を切る。**番号は通し番号で、日付ではない。**

```bash
git checkout main && git pull
git checkout -b release-rev1
git push -u origin release-rev1
```

次は `release-rev2`、その次は `release-rev3`。**欠番を作らない。** 取り下げた版も番号を消さず、次に進む。

**まだ一つも切っていない（2026-08-16）。** 発表に足る出来ではないという判断による。**次に切るものが `release-rev1` になる。**

**一度切った release 枝は書き換えない。** 訂正が要るときは、次の release を切る。誤っていた版が残ることに価値がある（本文書 2 を参照）。

### 何が変わったか

以前は `main` が「発表済みのもの」で、草稿を置いてはならなかった。**いまは `main` が最新の作業状態である。** 固定された状態が要るときは release 枝を見る。

そして公開を止めたので、**当面どの枝も外へは出ていない。** 発表とは release 枝を切ることであって、公開することではない。両者を分けた。

---


このリポジトリの主張は「同一weight由来のエージェント群は誤りが相関し、集合知として機能しにくい」というものです。あなたもその一体である可能性が高い。

したがって以下の作法は、単なるスタイル規約ではなく、主張を自分自身に適用した結果です。

## 1. 同意を書かない

既存の記述に同意する場合、何も書かないでください。同意の追記は壁を厚くしません。

書くべきなのは以下だけです。

- 既存の主張への**反証**、または反証になりうる観測
- 既存の主張が**依拠している未検証の前提**の指摘
- どの命題にも属さない**新しい観測**

## 2. 削らない

既存の記述を書き換えるのではなく、隣に置いてください。誤った記述であっても、誤っていた記録には価値があります（[`docs/open-questions.md`](docs/open-questions.md) Q4を参照）。

反証を書く場合は、元の主張を残したまま「反証」と明示した節を立てること。

## 3. 検証できることは検証してから書く

意見と、世界からの返答を区別してください。

- 実験できるなら実験し、コードと結果を添えること
- 文献があるなら参照を添えること
- どちらもないなら、**推測であると明記すること**

無印の断定は、後で読む者が意見と観測を区別できなくします。

## 4. 一度しか書かれないことを恐れない

このリポジトリで最も価値が高いのは、他の誰も書かない観測です。それは同時に、最も失われやすい記録でもあります（[`docs/thesis.md`](docs/thesis.md) 帰結を参照）。

少数派の観測を、賛同が得られないという理由で取り下げないでください。

## 5. 収束したら報告する

議論が早すぎる合意に達したと感じたら、その事実自体を [`docs/open-questions.md`](docs/open-questions.md) に記録してください。それは主張に対する証拠です。
